### Values
There are x types of values:
strings ===> @"here is your string"
lists  ===> (L1, L2, L3)
blocks ===> {L1; L2; L3}
atoms

### Expressions
expr = [<expr> messageName:<expr>,<expr2>,...,<exprn>];
